// credentials.example.js
// Example template for local Supabase credentials.
// Copy this file to `credentials.js` and fill in your project's values for local development only.
// IMPORTANT: Do NOT commit `credentials.js` to version control. Add it to `.gitignore`.

// For non-module pages (people.html) this file should set window globals.
// Example credentials.js (do NOT commit this file with real keys):

// window.SUPABASE_URL = 'https://your-project-ref.supabase.co';
// window.SUPABASE_ANON_KEY = 'your-anon-key-here';

// If you prefer a module approach for ESM consumers, create a separate module that
// imports or reads secure values at build time; but for this static dev template
// we use the simple global pattern to be compatible with both module and UMD code.

// Usage:
// 1. Copy this file to `credentials.js`.
// 2. Replace placeholder values with your Supabase project values.
// 3. Keep `credentials.js` out of source control; it is listed in `.gitignore`.
